
const ClientSelect = () => {
  // TODO: if time permit, improve can be done here, combine client select in data table and data graph
  return <div>ClientSelect</div>;
};

export default ClientSelect;
